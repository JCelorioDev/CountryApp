import { ObjectToArrayPipePipe } from './object-to-array-pipe.pipe';

describe('ObjectToArrayPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectToArrayPipePipe();
    expect(pipe).toBeTruthy();
  });
});
